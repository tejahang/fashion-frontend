# 💻 Fashion Shop Frontend - React UI (CN5006)

This directory contains the client-side React application for the Fashion Shop project. It provides a user interface to interact with the MongoDB database via a REST API.

---

## 📋 Prerequisites
Ensure you have the following ready before starting:
- **Node.js** (LTS version) installed on your machine.
- **The Backend Server**: The backend must be active on `http://localhost:5500` for the frontend to function.

---

## 📦 1. Dependencies Used
The following libraries are essential for this project and will be installed during the setup process:

* **react & react-dom**: The core library used to build the component-based user interface.
* **axios**: A promise-based HTTP client used to communicate with the Node.js backend endpoints.
* **react-router-dom**: Used to implement a Single Page Application (SPA) with navigation between different tasks.
* **react-scripts**: Standard scripts for starting, building, and testing the React application.

---

## ⚙️ 2. Installation & Setup

1.  **Navigate to the frontend folder**:
    ```bash
    cd fashion-frontend
    ```
2.  **Install all dependencies**:
    Run the following command to install the libraries listed above:
    ```bash
    npm install
    ```
    *Note: This command reads the `package.json` file and downloads all required packages into the `node_modules` folder.*

---

## 🚀 3. Running the Application

To launch the development server:
```bash
npm start
